<!DOCTYPE html>
<html lang="de">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <title>USA & Donald Trump Quiz</title>
  <style>
    body {
      font-family: Arial, sans-serif;
      background: #f0f0f0;
      padding: 20px;
      max-width: 600px;
      margin: auto;
    }

    h1 {
      text-align: center;
      color: #333;
    }

    .question-container {
      background: #fff;
      padding: 20px;
      margin-top: 20px;
      border-radius: 8px;
      box-shadow: 0 2px 6px rgba(0, 0, 0, 0.1);
    }

    .question {
      font-size: 18px;
      margin-bottom: 10px;
    }

    .answers {
      list-style: none;
      padding: 0;
    }

    .answers li {
      margin-bottom: 10px;
    }

    button {
      padding: 10px 20px;
      background: #007BFF;
      color: white;
      border: none;
      border-radius: 4px;
      cursor: pointer;
    }

    button:hover {
      background: #0056b3;
    }

    .result {
      text-align: center;
      font-size: 20px;
      margin-top: 30px;
    }
  </style>
</head>
<body>

  <h1>USA & Trump Quiz 🇺🇸</h1>

  <div class="question-container" id="quiz">
    <!-- Fragen werden hier eingefügt -->
  </div>

  <div class="result" id="result"></div>

  <script>
    const quizData = [
      {
        question: "In welchem Jahr wurde Donald Trump Präsident der USA?",
        answers: ["2015", "2016", "2017", "2018"],
        correct: 2
      },
      {
        question: "Wie lautet der Wahlslogan von Donald Trump 2016?",
        answers: [
          "Stronger Together",
          "Yes We Can",
          "Make America Great Again",
          "Build Back Better"
        ],
        correct: 2
      },
      {
        question: "Wie viele Bundesstaaten hat die USA?",
        answers: ["50", "48", "52", "51"],
        correct: 0
      },
      {
        question: "Welche Partei vertritt Donald Trump?",
        answers: ["Demokraten", "Republikaner", "Unabhängige", "Grüne"],
        correct: 1
      },
      {
        question: "Wo wurde Donald Trump geboren?",
        answers: ["Chicago", "New York City", "Los Angeles", "Washington D.C."],
        correct: 1
      }
    ];

    let currentQuestion = 0;
    let score = 0;

    const quiz = document.getElementById('quiz');
    const result = document.getElementById('result');

    function loadQuestion() {
      if (currentQuestion >= quizData.length) {
        showResult();
        return;
      }

      const qData = quizData[currentQuestion];
      quiz.innerHTML = `
        <div class="question">${qData.question}</div>
        <ul class="answers">
          ${qData.answers.map((answer, index) => `
            <li><button onclick="checkAnswer(${index})">${answer}</button></li>
          `).join('')}
        </ul>
      `;
    }

    function checkAnswer(selected) {
      const correct = quizData[currentQuestion].correct;
      if (selected === correct) {
        score++;
      }
      currentQuestion++;
      loadQuestion();
    }

    function showResult() {
      quiz.style.display = 'none';
      result.innerHTML = `
        🎉 Du hast ${score} von ${quizData.length} Fragen richtig beantwortet!
      `;
    }

    // Start
    loadQuestion();
  </script>

</body>
</html>
